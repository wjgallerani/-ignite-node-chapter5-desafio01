{
  "username": "postgres",
  "password": "docker",

  "name": "finApi",
  "type": "postgres",
  "host": "localhost",
  "port": 5432,
  "database": "finApi",
  "entities": [
    "./src/modules/**/entities/*.ts"
  ],
  "migrations": [
    "./src/database/migrations/*.ts"
  ],
  "cli": {
    "migrationsDir": "./src/database/migrations"
  }
}
